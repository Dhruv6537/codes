a="hello  i am   dhruv"
print(a.replace("  "," "))
print(a)
# Next string got changed but the main string is reamin unchanged 
# Strings are immutable which means that you cannot change them by running functions on them